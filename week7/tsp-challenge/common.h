#include <iostream>
#include <vector>
#include <fstream>  // ファイルの読み書き
using namespace std;

vector<vector<double> > read_input(string);
string format_tour(vector<int>);
void print_tour(vector<int>);
